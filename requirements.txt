babeldoc==0.2.33
fonttools==4.63.0
numpy==2.2.6
pdfminer-six==20250416
pikepdf==10.12.0
pymupdf==1.25.2
requests==2.34.2
tenacity==9.1.4
tqdm==4.70.0

# Imported directly by the bundled core but previously reached only through
# babeldoc, so nothing pinned them. Not the headless opencv build: babeldoc
# requires the full package, and installing both just shadows one with the other.
onnx==1.17.0
onnxruntime==1.20.1
opencv-python==5.0.0.93
peewee==3.17.9

# LLM support
openai>=1.0.0
python-dotenv>=1.0.0
